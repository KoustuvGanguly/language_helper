name: language_helper
description: Control multiple languages with an efficient generator and a custom GPT-4 translator for easy localization.
version: 0.11.7
homepage: https://github.com/lamnhan066/language_helper
publish_to: none
topics:
  - localization
  - internationalization
  - language
  - i18n
  - l10n

environment:
  sdk: ">=3.7.0 <4.0.0"

platforms:
  android:
  ios:
  windows:
  linux:
  macos:
  web:

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  language_code: ">=0.4.0 <0.6.0"
  language_helper_generator:
    git:
      url: https://github.com/KoustuvGanguly/language_helper_generator_custom.git
      ref: main
  shared_preferences: ^2.0.0
  http: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
